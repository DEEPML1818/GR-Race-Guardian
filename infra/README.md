Dockerfiles included for demonstration. `docker-compose.yml` references simple builds — adjust contexts before using.

On Windows with Docker Desktop:
```cmd
cd "d:\Working folder with race tracks and timing\gr-race-guardian"
docker-compose up --build
```
